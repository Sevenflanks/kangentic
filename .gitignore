# Claude Code

# Per-project data (runtime databases, sessions, worktrees)
.kangentic/

# Agent runtime config directories (injected hooks, settings)
.codex/
.gemini/
.aider/

# Stray bin shims left behind by accidental `npm install -g <agent-cli>`
# inside a worktree (npm prefix pointing at cwd emits the trio here instead
# of node_modules/.bin/). Anchored so they only match repo/worktree roots.
/gemini
/gemini.cmd
/gemini.ps1
/claude
/claude.cmd
/claude.ps1
/codex
/codex.cmd
/codex.ps1
/qwen
/qwen.cmd
/qwen.ps1
/opencode
/opencode.cmd
/opencode.ps1
/aider
/aider.cmd
/aider.ps1
/kimi
/kimi.cmd
/kimi.ps1
/droid
/droid.cmd
/droid.ps1
/copilot
/copilot.cmd
/copilot.ps1
/warp
/warp.cmd
/warp.ps1

# Superpowers scratch and process docs (ledgers, briefs, diffs, reports, plans, specs).
# Process artifacts, not project docs - keep them out of the repo.
.superpowers/
docs/superpowers/

# Build output
dist/


# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock
.DS_Store

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test

# parcel-bundler cache (https://parceljs.org/)
.cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Webpack
.webpack/

# Vite
.vite/

# Electron-Forge
out/

# Test artifacts
tests/screenshots/
tests/.tmp/
tests/.tmp-template/
tests/.test-data/
tests/reports/
tests/fixtures/agent-pty/
test-results/
playwright-report/
e2e-timings*.json
.kangentic-test-sandbox-*
.claude/settings.local.json
.claude/scheduled_tasks.lock
kangentic.local.json

# Marketing captures output (regenerated on release tags)
/captures/
tests/captures/fixtures/scrollback-*.json

# Ad-hoc empirical capture traces (e.g. PTY output, parser probes). These
# are written by scripts/verify-*.js / scripts/capture-*.js / scripts/probe-*.js
# for local bug-reproduction work and are not checked in. Probe reports carry
# machine-specific paths, so they must never enter this public repo.
/debug-traces/
*-probe-report.md
.opencode/plugins/kangentic-activity.js
